import 'package:flutter/widgets.dart';

const kDefaultInDuration = Duration(milliseconds: 100);
const kDefaultWaitDuration = Duration(milliseconds: 200);
const kDefaultOutDuration = Duration(milliseconds: 400);

const kDefaultInCurve = Curves.fastOutSlowIn;
const kDefaultOutCurve = Curves.easeOut;

const kDefaultFromColor = Color(0x01FFFFFF);
const kDefaultToColor = Color(0xFFEEEEEE);

const kDefaultBorderRadius = BorderRadius.zero;

const kDefaultUpdateAfterHighlighting = false;
